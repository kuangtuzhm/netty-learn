# log
logging.path=logs
logging.level.root=INFO
logging.level.com.jimi=INFO

# server
server.port=8081
server.context-path=/

netty.port=8222

endpoints.shutdown.enabled=true
endpoints.shutdown.sensitive=false